{
  "name": "ha-ze-gs",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --coverage",
    "build": "node build.js"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.1.1",
    "vitest": "^1.6.0"
  }
}
